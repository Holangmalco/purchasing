name: Keep Streamlit Alive

on:
  schedule:
    - cron: "0 */8 * * *"

  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Wake up Streamlit App
        run: |
          curl -I --max-time 300 "https://iacf-purchasing.streamlit.app/" || true